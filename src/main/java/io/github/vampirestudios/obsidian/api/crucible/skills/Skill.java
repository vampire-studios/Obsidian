/*
package io.github.vampirestudios.obsidian.api.crucible.skills;

import io.lumine.mythic.api.adapters.AbstractEntity;
import io.lumine.mythic.api.adapters.AbstractLocation;
import io.lumine.mythic.api.config.MythicConfig;
import io.lumine.mythic.core.skills.SkillCondition;
import java.util.Collection;
import java.util.HashSet;
import java.util.List;

public interface Skill {
  String getInternalName();
  
  MythicConfig getConfig();
  
  Collection<SkillHolder> getParents();
  
  void addParent(SkillHolder paramSkillHolder);
  
  boolean isInlineSkill();
  
  void execute(SkillTrigger paramSkillTrigger, SkillCaster paramSkillCaster, AbstractEntity paramAbstractEntity, AbstractLocation paramAbstractLocation, HashSet<AbstractEntity> paramHashSet, HashSet<AbstractLocation> paramHashSet1, float paramFloat);
  
  void execute(SkillMetadata paramSkillMetadata);
  
  boolean isUsable(SkillMetadata paramSkillMetadata);
  
  boolean isUsable(SkillMetadata paramSkillMetadata, SkillTrigger paramSkillTrigger);
  
  boolean onCooldown(SkillCaster paramSkillCaster);
  
  List<SkillCondition> getConditions();
  
  List<SkillCondition> getConditionsTarget();
  
  List<SkillCondition> getConditionsTrigger();
}*/
