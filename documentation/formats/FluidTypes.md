# Fluid Types

Obsidian does not currently expose a standalone fluid type registry.

Instead, fluid behavior presets are controlled through the `parent` field on fluid definitions. See [Fluids](./Fluids.md) for details on using `WATER`, `LAVA`, or `NONE` as a base.